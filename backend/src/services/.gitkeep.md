This folder is reserved for business-logic services as Phase 2/3 features
land (e.g. search indexing, export generation, share-link creation) — kept
separate from controllers/ so route handlers stay thin. Empty for now; only
highlights sync exists so far, and its logic lives directly in
controllers/highlights.controller.ts since there isn't enough of it yet to
warrant a separate layer.
